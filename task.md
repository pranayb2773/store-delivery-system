
# PHP Developer Technical Test

Welcome! This assessment evaluates your backend development skills using PHP 8.1+ (Laravel 10+ preferred).

**Goal:** Build a backend service for store management, inventory, and order routing for a quick-commerce platform. Estimated Time: 3–4 hours (do not spend more than 4 hours).

**Requirements:**

- "Implement console command import:postcodes to import UK postcode
  data with lat/lon"
- "API endpoints:"
  - "POST /api/stores (store creation)"   		
  - "GET /api/stores/nearby (nearby stores discovery)"   		
  - "GET /api/stores/can-deliver (delivery feasibility)"
- "Database: relational DB (MySQL/PostgreSQL), proper indexes for geospatial queries, migrations, seeders"
- "Code quality: PSR-12, type hints, error handling, separation of concerns"
- "Tests: unit + integration tests, edge cases"
- "Documentation: README with setup, API docs, database schema diagram, reflection on architecture"

**Optional / Bonus:** Docker setup, caching, operating hours, delivery time estimation, API authentication, performance benchmarks

**Submission:**
- "GitHub/GitLab repository with code"
- "README and documentation"
- "Tests passing"
